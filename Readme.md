# Stallings Foldings for Subgroups of Amalgams

A Python implementation of the generalized Stallings-folding algorithm for finitely generated subgroups of amalgams of finite groups.

The project implements the six-step construction described in Section 8 of:

> L. Markus-Epstein, *Stallings' Foldings and Subgroups of Amalgams of Finite Groups*.

The implementation works with finite groups represented by labelled Cayley graphs and constructs reduced precovers representing finitely generated subgroups of an amalgam.

## What this project does

The implementation provides labelled-graph operations required by the construction, including:

* labelled graph construction and traversal
* graph folding and vertex identification
* monochromatic component detection
* subgroup/stabilizer computations
* Cayley and relative Cayley graph constructions
* construction of reduced precovers for finitely generated subgroups

The main algorithm follows the six-step procedure from the paper.

## Mathematical setting

Given finite groups \(G_1\) and \(G_2\), together with an identified subgroup

$$
A \leq G_1,\qquad A \leq G_2,
$$

the program works with the amalgam

$$
G_1 *_A G_2
$$

and constructs a finite labelled graph representing a finitely generated subgroup.

Finite factors are represented explicitly by their Cayley graphs. This makes the implementation closely reflect the combinatorial construction in the paper.

## Example

A simple example using a finite dihedral group and a cyclic group:

```python
from stallings_amalgams import Graph

# construct finite factor Cayley graphs
# ...

H = [
    ["b"],
    ["a", "b", "a^-1"],
    ["a", "c", "a", "c", "a", "c"],
]

P = Graph.get_red_precover(G1, G2, A, H)
```

The resulting graph is the reduced precover associated with the subgroup generated by `H`.

## Project status

This is an independent research-software project accompanying my study and implementation of the algorithm in Markus-Epstein's paper.

The implementation is currently being validated against additional explicit examples and small finite-group constructions.

## Why this project?

The project combines:

* abstract algebra and combinatorial group theory
* graph algorithms
* finite group computations
* algorithm implementation in Python
* mathematical verification through concrete examples

## References

L. Markus-Epstein, *Stallings' Foldings and Subgroups of Amalgams of Finite Groups*.

See the accompanying documentation for implementation details and correspondence between the mathematical construction and the code.
